package pages;

import io.restassured.response.Response;
import static io.restassured.RestAssured.given;
public class AddPlace {

	public Response addPlace(String requestBody,String apiKey) {
		return given().header(null);
		
	}
}
